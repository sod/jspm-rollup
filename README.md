```sh
npm install # install npm and jspm dependencies
bin/create-child-components.sh 100 # create 100 child components
bin/build.sh # build via typescript, jspm, rollup
bin/serve.sh # test build in the browser on localhost:3000
```
